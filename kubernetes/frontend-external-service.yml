apiVersion: v1
kind: Service
metadata:
  name: translator-frontend-service
  namespace: frontend-translator
spec:
  selector:
    app: frontend-translator
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
